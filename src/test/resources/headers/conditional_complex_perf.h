// Preprocessed output with HIGH_PERF defined but not ENABLE_CACHE
// Original: conditional_complex.h with -DHIGH_PERF

struct PerformanceConfig {
    uint8 priority;
    uint24 threshold;
};

struct GlobalSettings {
    uint8 version;
    uint8 checksum;
};
